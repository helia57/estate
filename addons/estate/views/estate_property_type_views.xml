<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- LIST VIEW -->
    <record id="view_estate_property_type_list" model="ir.ui.view">
        <field name="name">estate.property.type.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="offer_count" readonly="1"/>
            </list>
        </field>
    </record>

    <!-- FORM VIEW -->
    <record id="view_estate_property_type_form" model="ir.ui.view">
        <field name="name">estate.property.type.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form string="Property Type">
                <header>
                    <button name="%(action_estate_property_offer_by_type)d"
                            type="action"
                            class="oe_stat_button"
                            icon="fa-tag">
                        <field name="offer_count" widget="statinfo" string="Offers"/>
                    </button>

                </header>
                <sheet>

                    <group>
                        <field name="name"/>
                        <field name="sequence"/>
                    </group>

                    <notebook>
                        <page string="Properties">
                            <field name="property_ids"
                                context="{'tree_view_ref': 'estate.view_estate_property_list_inline'}"/>
                                

                        </page>
                    </notebook>

                </sheet>
            </form>
        </field>
    </record>

    <!-- ACTION -->
    <record id="action_estate_property_type" model="ir.actions.act_window">
        <field name="name">Property Types</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="action_estate_property_offer" model="ir.actions.act_window">
        <field name="name">Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">
            [('property_type_id', '=', active_id)]
        </field>
    </record>


</odoo>
