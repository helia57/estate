<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- ============================= -->
    <!--           LIST VIEW           -->
    <!-- ============================= -->
    <record id="view_estate_property_offer_list" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="Offers"
                  decoration-success="status == 'accepted'"
                  decoration-danger="status == 'refused'">
                <field name="price" decoration-danger="is_low"/>
                <field name="property_type_id"/>
                <field name="property_id"/>
                <field name="partner_id"/>
                <field name="validity"/>
                <field name="date_deadline"/>

                <button name="action_accept"
                        type="object"
                        icon="fa-check"
                        title="Accept"
                        invisible="status != 'new'"/>

                <button name="action_refuse"
                        type="object"
                        icon="fa-times"
                        title="Refuse"
                        invisible="status != 'new'"/>

                <field name="status"/>
            </list>
        </field>
    </record>

    <!-- ============================= -->
    <!--           FORM VIEW           -->
    <!-- ============================= -->
    <record id="view_estate_property_offer_form" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Offer">
                <sheet>

                    <group>
                        <field name="price"/>
                        <field name="status" readonly="1"/>
                    </group>

                    <group string="Validity">
                        <field name="validity"/>
                        <field name="date_deadline"/>
                    </group>

                    <group>
                        <field name="partner_id"/>
                        <field name="property_id"/>
                    </group>

                    <footer>
                        <button name="action_accept"
                                type="object"
                                string="Accept"
                                class="btn-primary"
                                invisible="status != 'new'"/>

                        <button name="action_refuse"
                                type="object"
                                string="Refuse"
                                class="btn-secondary"
                                invisible="status != 'new'"/>

                        <button string="Close"
                                special="cancel"
                                class="btn-link"/>
                    </footer>

                </sheet>
            </form>
        </field>
    </record>

    <!-- ============================= -->
    <!--           ACTIONS             -->
    <!-- ============================= -->

    <!-- Menu: all offers -->
    <record id="action_estate_property_offer_all" model="ir.actions.act_window">
        <field name="name">Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Button stat: offers by property type -->
    <record id="action_estate_property_offer_by_type" model="ir.actions.act_window">
        <field name="name">Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
    </record>

</odoo>

